# ResortsLite - Cloud-Ready Configuration
spring.application.name=ResortsLite

# Dynamic port assignment for cloud environments (ECS/EKS)
server.port=${SERVER_PORT:8080}

# Database configuration - externalized to environment variables
spring.datasource.url=${DB_URL:jdbc:h2:mem:resortdb;DB_CLOSE_DELAY=-1}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}

spring.h2.console.enabled=${H2_CONSOLE_ENABLED:false}
spring.h2.console.path=/h2-console

# Redis configuration for distributed session management (ElastiCache)
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.session.store-type=redis
spring.session.redis.namespace=resortslite:session

# Cloud-native service endpoints - externalized to AWS Parameter Store
app.payment.endpoint=${PAYMENT_ENDPOINT:http://localhost:9090/charge}
app.inventory.endpoint=${INVENTORY_ENDPOINT:http://localhost:8081/rooms}
app.notification.endpoint=${NOTIFICATION_ENDPOINT:http://localhost:7070/send}

# AWS S3 configuration for file storage
aws.s3.bucket.name=${S3_BUCKET_NAME:resortslite-reports}
aws.s3.region=${AWS_REGION:us-east-1}

# AWS Secrets Manager configuration
aws.secrets.db-credentials=${DB_CREDENTIALS_SECRET:resortslite/db/credentials}

# AWS Systems Manager Parameter Store prefix
aws.ssm.parameter.prefix=${SSM_PARAMETER_PREFIX:/resortslite}
