apiVersion: v1
kind: Service
metadata:
  name: bookingcomp-service
  namespace: bookingcomp
  labels:
    app: bookingcomp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: bookingcomp
